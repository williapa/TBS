{
  "name": "@TBS/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider-neutral game-session ports, use cases, and reconciliation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@TBS/game-core": "workspace:*",
    "@TBS/game-rules": "workspace:*",
    "@TBS/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "nx": {
    "projectType": "library",
    "tags": ["layer:application", "type:library"]
  }
}
