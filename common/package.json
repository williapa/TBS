{
  "name": "@TBS/common",
  "version": "1.0.0",
  "description": "Shared contracts and deterministic engine for the TBS web client and Supabase backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "pnpm build && node --test dist/*/*.test.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@TBS/game-core": "workspace:*",
    "@TBS/game-rules": "workspace:*",
    "@TBS/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "6.0.3"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "author": "Paul Williams",
  "license": "UNLICENSED",
  "nx": {
    "tags": ["layer:domain", "type:library"]
  }
}
