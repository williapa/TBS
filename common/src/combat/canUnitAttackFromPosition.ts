import { MapItem } from "../types";

// maybe I should do this differently
const canUnitAttackFromPosition = (unit: string, map: MapItem[][], index: number) => {
  return true; // todo: implement 
};

export default canUnitAttackFromPosition;
