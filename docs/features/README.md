# Feature Documentation

`supabase-realtime-migration.md` is the implementation record for the current architecture. The other feature files preserve the behavior and design history of individual game mechanics; where an older file mentions a server-era implementation detail, `docs/architecture.md`, `docs/game-domain.md`, and `docs/testing.md` are authoritative for the supported system.
